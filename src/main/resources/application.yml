spring:
  application:
    name: wavit
  profiles:
    group:
      test: test
      local: local, datasource
      prod: prod, datasource
    include:
      - actuator
    active: ${SPRING_PROFILES_ACTIVE:local}
